Before Milestone 3, close these items from the M2 report:

1. Every import shows 0 rejected rows. docs/MILESTONES.md requires the generator to inject messiness: case/whitespace/gmail-dot variants, phone-only matches, blocklisted junk emails, duplicate rows, invalid phones, and mixed date formats. Report, per seed file, how many of each messiness type the generator injected (write generator_stats.json next to ground_truth.json) versus how the import handled them: rejected, warning, deduplicated, or normalized. If any type was not injected, add it. Add at least one deliberately unrecoverable row type per file (for example a gift with a non-numeric amount and no identifier) so rejection paths are exercised, and confirm those rows land in the error CSV.
2. Show warning counts in the import list and detail alongside ok/rejected (invalid phone, invalid email, blocklisted identifier, date format coerced).
3. Report wall-clock time per import for the small seed, then run a 1,000,000-row gift import and report rows per second. Target from docs/PRD.md: 1M rows under 15 minutes. If it misses, profile and fix before continuing.
4. Confirm ground_truth.json, generator_stats.json, and generated seed CSVs are written outside the repo or are gitignored.
5. Remove milestone references from UI copy. Empty states should describe what the user can do, for example on Dashboard: "No data yet. Import a source to populate dashboards." Pages for features not built yet can say "Not available yet" without naming milestones.

Then build Milestone 3 exactly as written in docs/MILESTONES.md and docs/IDENTITY_RESOLUTION.md:
- Resolver with union-find over batches, merge handling (re-point identifiers, source records, gifts, events, consents with opt-out precedence, enrichment values), survivorship by source priority with address blocks kept together, blocklist, and the high-cardinality auto-blocklist guard.
- identity.resolve_batch enqueued automatically after imports and event ingestion, with advisory locks per identifier.
- Profiles list with trigram search and filters (donor status placeholder until traits exist, source, has email, has phone), and profile detail with Overview, Gifts, Activity, Identity (identifiers, source records with priority, merge history), and Enrichment tabs. Mask email and phone for the viewer role in the API. Write profile.view to the audit log.
- Data Health dashboard tab: pending resolution count, merges per day, blocklist hits, auto-blocklisted identifiers with admin approve/unblock actions, rejected rows by recent import.
- Every test listed under "Required tests" in docs/IDENTITY_RESOLUTION.md.

M3 acceptance report must include, against ground_truth.json on the small seed:
- true person count, resolved active profile count, and percentage difference (target within 2%)
- share of profiles containing records from more than one true person, split into intentional shared-household cases and real false merges (target under 1% real false merges)
- number of true people split across more than one profile, with the top 5 reasons (for example email variant not normalized, phone-only record with invalid phone)
- total merges performed and resolution job wall-clock time
- result of re-importing all seed files: zero new profiles and zero merges

Then rerun the full seed load on a clean database and give me the numbers. List every new test by name.