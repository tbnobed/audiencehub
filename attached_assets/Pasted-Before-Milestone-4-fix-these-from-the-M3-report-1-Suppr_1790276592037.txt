Before Milestone 4, fix these from the M3 report:

1. Suppression scope bug (spec correction, update docs/DATA_MODEL.md and docs/SECURITY_PRIVACY.md to match): only suppressions with reason deletion_request block rows at import time. Suppressions with reason hard_bounce, spam_complaint, or manual must not reject any import rows. They set email consent to opted_out and exclude the address from email-channel activations only. Gifts, events, and enrichment are never rejected because of a hard bounce. Add tests: a hard-bounced donor's gifts import and count toward traits; a deletion-request identifier still blocks re-import. Rerun the clean seed load and report the new rejected counts per file.
2. Warning noise: 315,998 warnings on 470k gifts is too many to be useful. A date that parses cleanly in the format declared in the import mapping (options.date_format) is not a warning. Only warn when a value needed a fallback format, was ambiguous (for example 03/04/2024 with no declared format), or was clamped. Report the warning breakdown by type per file after the fix.
3. Complete the M3 report: top 5 reasons for the 128 split true people with counts, the details of the one false merge (which identifier linked them and why the blocklist or household rules didn't catch it), total merges performed, and resolution job wall-clock time.

Then build Milestone 4 exactly as written in docs/MILESTONES.md:
- Trait registry in app/traits/registry.py with label, type, description, and SQL for every trait in docs/DATA_MODEL.md. Set-based recompute only (INSERT ... SELECT ... ON CONFLICT DO UPDATE), with a pinned as_of parameter, a dirty-profile incremental mode after imports (debounced 10 minutes), and the nightly full recompute via the scheduler.
- trait_snapshots written on the first recompute of each month, and back-filled for the last 24 months from gift history on the seed.
- Dashboard endpoints and pages per docs/API.md and docs/UI.md: Overview, Giving, Retention, Engagement, Sources (Data Health already exists). Date range picker with presets, prior-period deltas on KPI tiles, chart/table toggle, and CSV download for analysts with an audit log entry.
- Profiles list and detail now show donor status, RFM, LTV, and last gift from traits. Wire the donor status filter.

M4 acceptance report:
- Trait values match a slow, straightforward Python reference implementation on 1,000 random seeded profiles (report any mismatches by trait).
- Full recompute wall-clock time on the small seed, and on the medium seed (500k profiles) if the environment can run it.
- Screenshot of each dashboard tab with the seeded data.
- Sanity numbers: donor count by donor_status, 12-month giving total, and November plus December share of annual giving (the generator seeds year-end seasonality, so this should clearly exceed other months).
- List every new test by name, including donor status boundary tests at 365/366/730/731 days and RFM quintile tests.