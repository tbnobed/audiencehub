Production issues on commit c85dd6d at 500k profiles. Fix all of them, with tests, then give me the server upgrade commands.

1. Slow Overview dashboard. GET /api/dashboards/overview stays pending for 10+ seconds or never returns; Postgres logs show statement timeouts and the email-consent UNION query (profiles.email + source_records.email_norm + identifiers, joined on lower(btrim(email))) running until the client disconnects.
   - Precompute. Add rollup tables refreshed by traits.recompute (nightly and after each import), never computed per request:
     dashboard_daily(day, source_id, fund, campaign, channel, gift_count, gift_amount, donor_count, new_donor_count)
     dashboard_kpis(as_of, key, value) for profile counts, partners by status, recurring partners, email opted-in, lapsing.
     Endpoints aggregate these small tables for any date range. No dashboard query may touch raw gifts, events or source_records.
   - Consent comes only from the consents table. Populate it during import (email_consent columns, ESP consent imports, hard bounces) and during identity merges. Index consents(channel, status).
   - Shared cache. Replace the per-process in-memory cache with a dashboard_cache table (key, payload jsonb, computed_at) shared by all API workers. Serve cached results immediately and refresh in the background. Invalidate after traits.recompute.
   - Progressive loading. Split the overview into one request per card (kpis, giving-by-month, needs-attention, campaigns, partner-status). Each card renders when its data arrives, with a per-card error state and a Retry button instead of an endless skeleton. Client timeout 15 s with a clear message.
   - Target: every /api/dashboards/* endpoint under 300 ms at 500k profiles. Add "kinship benchmark --dashboards" that prints the time for each endpoint without needing a browser session.

2. Data Health sidebar badge shows "500,000". It must show the number of open issues (failed jobs, unreviewed rejected-row reports, pending auto-blocklist reviews, enrichment expiring within 60 days).

3. The built CSS contains data: URI woff2 fonts that fail to load (8 console errors). Remove them; keep only the bundled .woff2 files.

4. Scheduler enqueue fails every tick with "there is no unique or exclusion constraint matching the ON CONFLICT specification". app/jobs/queue.py uses index_where=Job.status.in_(("queued", "running")), which renders as bind parameters that Postgres cannot match to the partial index. Change it to index_where=text("status IN ('queued', 'running')"), make sure the migration's partial unique index uses the identical literal predicate, add a Postgres test that enqueues the same dedupe_key twice and gets one row with no error, and add a boot self-check that enqueues and cancels a noop job.

5. Add "kinship load-status [--wait]": a progress line every 30 s; a final summary with per-import ok/rejected/warn and rows/s, profiles, merges, gifts, total wall-clock and PASS/FAIL; exit code 0 on PASS. "kinship seed ... --load" calls it with --wait automatically.

6. Postgres settings in docker-compose.yml: add -c checkpoint_timeout=15min -c max_wal_size=16GB -c wal_compression=on.

Report: which commit covers each item, the tests added, and the "kinship benchmark --dashboards" output at 500k (or 250k if this environment can't hold 500k; say which).