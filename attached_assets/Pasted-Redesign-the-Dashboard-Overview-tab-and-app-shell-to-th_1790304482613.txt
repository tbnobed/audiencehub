Redesign the Dashboard Overview tab and app shell to this spec (Kinship brand tokens from kinship-brand/tokens.json):

Shell
- Sidebar groups with 11px uppercase labels: INSIGHTS (Dashboard, Profiles, Segments), OPERATE (Activations, Imports, Sources, Data Health), SETTINGS (Admin, System). Active item: signal-soft fill + signal text, no outline. Count badges: Imports shows "N running"; Data Health shows a danger-tinted count of open issues.
- Sidebar footer card for the active import: name, percent, 4px progress bar, "done / total rows · rows/s". Hidden when nothing runs.
- Top bar: search (placeholder "Search partners by name, email or phone", "/" hint chip), one combined amber badge "DEV · DEV AUTH", theme toggle, user avatar with initials, name and role.

Overview page
- Header: "Overview" (Sora 22/28) + subtitle "Giving, partners and engagement across every source." No eyebrow. Right side: one segmented date control (30 d, 90 d, 12 m, YTD, Last FY, Custom…) with one line under it: "<start> – <end> · compared with the prior <period>". Remove the separate Current/Prior line and the OVERVIEW dropdown.
- Row 1: four KPI tiles: Giving, Active partners, Partner retention (YoY %), Average gift. Each: uppercase label, delta chip (▲ green / ▼ red with the word-free arrow plus value; "New" when the prior period is zero, never "0.0%"), 30px mono value, 12-bar sparkline of monthly values (last bar highlighted), one muted context line.
- Row 2: one strip panel with four inline stats: Profiles, Recurring partners (with change this month), Email opted in (with % of profiles), Lapsing (with change this month, warn color).
- Row 3: "Giving by month" (2/3 width): paired bars this period vs prior period, Nov–Dec bars in a lighter cyan, y-axis in $M, legend, and a one-line takeaway computed from data (share of giving in the top two months). Beside it "Needs attention" (1/3): up to 5 items ranked by severity (error, warning, notice, healthy), each with a severity stripe and label, title, one-line explanation, and a link to the fix (reactivation segment, import error report, affected segments, System page).
- Row 4: "Top campaigns" table (2/3): campaign, share-of-giving bar, gifts, avg gift, total; link to all campaigns. "Partner status" (1/3): 100% stacked bar of givers by status (Active, New, Reactivated, Lapsing, Lapsed), a legend list with share and count, and prospects shown separately as "Prospects who have never given".
- Empty states: compact (no 300px empty boxes); a chart with no data collapses to a one-line message with an action.

Data correctness (fix before styling)
- Every KPI counts active resolved profiles (active_profiles view), never source records. All-time partners and email opt-ins can never exceed active profiles.
- Giving and active-partner KPIs read gifts joined to resolved profiles; recompute dashboard caches after traits.recompute.
- Deltas: show "New" when the prior value is 0 and "—" when both are 0.
Use "partners" in all copy, never "donors".